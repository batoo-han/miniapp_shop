# Telegram-бот для Mini App

Бот предназначен для входа в витрину товаров: при `/start` показывает приветствие и устанавливает кнопку **«Каталог»** в меню чата, которая открывает Mini App.

## Текст приветствия

При команде `/start` бот отправляет:

> 🛒 Это тестовый магазин с витриной товаров.
>
> Для запуска витрины нажмите на кнопку **«Каталог»** в меню.

## Как правильно открывать Mini App (важно)

По документации Telegram Mini App (Web App) корректно открывается из **приватного чата** с ботом через кнопки типа `web_app`:

- **Menu button**: `setChatMenuButton(MenuButtonWebApp(...))` — кнопка **«Каталог»** в меню чата (внизу слева), открывает витрину.

## Настройка

### 1. Создать бота в BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram.
2. Отправьте `/newbot` и следуйте инструкциям.
3. Сохраните выданный токен (например, `123456:ABC-DEF...`).

### 2. Обязательно: разрешить домен для Mini App (BotFather /setdomain)

Если домен не разрешён, Telegram может **вообще не открывать** Mini App из кнопки `web_app` (при этом по прямой ссылке в чате сайт может открываться, потому что это уже “обычный браузерный” переход).

В BotFather:

1. Отправьте команду `/setdomain`
2. Выберите вашего бота
3. Укажите домен без пути, например: `app.batoohan.ru`

### 3. Рекомендуется: включить Mini App у бота (Bot Settings → Configure Mini App)

Это не строго обязательно для кнопки меню, но помогает Telegram “узнавать” ваш Mini App как Mini App (профильная кнопка запуска, превью, настройки загрузочного экрана и т.п.).

В BotFather:

1. `/mybots` → выберите бота
2. **Bot Settings** → **Configure Mini App**
3. **Enable Mini App**
4. Укажите URL Mini App: `https://app.batoohan.ru/miniapp/`

### 4. Переменные окружения

В `.env` в корне проекта:

```bash
BOT_TOKEN=123456:ABC-DEF...
MINIAPP_URL=https://app.batoohan.ru/miniapp/
```

### 5. Запуск

**Docker:**
```bash
docker compose up -d bot
```

**Локально:**
```bash
cd services/bot
python -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r requirements.txt
export BOT_TOKEN=...
export MINIAPP_URL=https://...
python main.py
```

## Структура

- `services/bot/main.py` — точка входа, обработчик `/start`, установка меню.
- `services/bot/requirements.txt` — зависимости (`python-telegram-bot`).
