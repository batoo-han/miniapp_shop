# Telegram-бот для Mini App

Бот предназначен для входа в витрину товаров: при `/start` показывает приветствие и даёт пользователю кнопку **«Каталог»** для запуска Mini App.

## Текст приветствия

При команде `/start` бот отправляет:

> 🛒 Это тестовый магазин с витриной товаров.
>
> Для запуска витрины нажмите на кнопку **«Каталог»**.
>
> Если кнопка не открывает витрину, откройте ссылку: `MINIAPP_URL`

## Как правильно открывать Mini App (важно)

По документации Telegram Mini App (Web App) корректно открывается из **приватного чата** с ботом через кнопки типа `web_app`:

- **Reply keyboard**: `KeyboardButton(web_app=WebAppInfo(...))` — рекомендованный способ запуска.
- **Menu button**: `setChatMenuButton(MenuButtonWebApp(...))` — работает в приватных чатах, но в разных клиентах может вести себя по-разному.

Поэтому бот делает **два пути запуска**:

- **Основной**: кнопка **«Каталог»** над полем ввода (reply keyboard, тип `web_app`).
- **Запасной**: ссылка в тексте сообщения (на случай, если у конкретного клиента Telegram кнопка не сработает).

## Настройка

### 1. Создать бота в BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram.
2. Отправьте `/newbot` и следуйте инструкциям.
3. Сохраните выданный токен (например, `123456:ABC-DEF...`).

### 2. Переменные окружения

В `.env` в корне проекта:

```bash
BOT_TOKEN=123456:ABC-DEF...
MINIAPP_URL=https://app.batoohan.ru/miniapp/
```

### 3. Запуск

**Docker:**
```bash
docker compose up -d bot
```

**Локально:**
```bash
cd services/bot
python -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r requirements.txt
export BOT_TOKEN=...
export MINIAPP_URL=https://...
python main.py
```

## Структура

- `services/bot/main.py` — точка входа, обработчик `/start`, установка меню.
- `services/bot/requirements.txt` — зависимости (`python-telegram-bot`).
